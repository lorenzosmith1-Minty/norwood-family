/// Superseded by `lib/board-scope.mo` (Tenancy 1C-C1).
///
/// The legacy family-agnostic board domain functions were replaced by the
/// canonical family-scoped implementations in `lib/board-scope.mo`, so exactly
/// one implementation of every board read and mutation exists. This module is
/// intentionally empty and is no longer imported by `main.mo`.
module {};
