/// Superseded by `mixins/board-scope-api.mo` (Tenancy 1C-C1).
///
/// The legacy board endpoints and their authorization helpers were replaced by
/// the canonical family-scoped endpoints in `mixins/board-scope-api.mo`, so
/// exactly one implementation of every board endpoint and every board
/// authorization check exists. This mixin is intentionally empty and is no
/// longer included by `main.mo`.
mixin () {};
