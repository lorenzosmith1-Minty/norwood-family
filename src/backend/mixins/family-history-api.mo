/// Superseded by Tenancy 1C-D3-A and 1C-D4-A.
///
/// The Story endpoints that used to live here are now the canonical
/// family-scoped `mixins/family-history-scope-api.mo`. The Mystery endpoints
/// that used to live here are now the canonical family-scoped
/// `mixins/mystery-scope-api.mo`. This mixin is intentionally inert so exactly
/// one implementation of every Story and Mystery endpoint exists.
mixin () {};
