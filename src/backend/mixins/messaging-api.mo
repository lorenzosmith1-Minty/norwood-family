// Superseded by `mixins/messaging-scope-api.mo` in Tenancy 1C-C3.
//
// The canonical family-scoped private Messaging API and its TEMPORARY Tenancy
// 1C compatibility wrappers now live in `mixins/messaging-scope-api.mo`, so
// exactly one implementation of every messaging read, mutation, and
// authorization check exists. This module is intentionally inert and is no
// longer included by `main.mo`.
mixin () {};
