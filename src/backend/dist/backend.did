type _ImmutableObjectStorageRefillResult = 
 record {
   success: opt bool;
   topped_up_amount: opt nat;
 };
type _ImmutableObjectStorageRefillInformation = record {
                                                  proposed_top_up_amount:
                                                   opt nat;};
type _ImmutableObjectStorageCreateCertificateResult = 
 record {
   blob_hash: text;
   method: text;
 };
type Value = 
 variant {
   "bool": bool;
   float: float64;
   "int": int;
   "nat": nat;
   "null";
   "text": text;
 };
type UserRole = 
 variant {
   admin;
   guest;
   user;
 };
type Timestamp = int;
type TimelineLinkTarget = 
 variant {
   ArchiveItem: nat;
   Mystery: MysteryId;
   Person: text;
   Story: StoryId;
 };
type TimelineEventType = 
 variant {
   Birth;
   CensusDocument;
   Death;
   FamilyEvent;
   Location;
   Marriage;
   Migration;
   MilitaryService;
   Mystery;
   PhotoDocument;
   Story;
 };
type TimelineEvent = 
 record {
   description: text;
   era: opt text;
   eventType: TimelineEventType;
   evidenceStatus: EvidenceStatus;
   id: text;
   linkTarget: TimelineLinkTarget;
   title: text;
   year: opt nat;
 };
type SuccessorStatus = 
 variant {
   Activated;
   Designated;
   Removed;
 };
type SuccessorDesignation = 
 record {
   assignedAt: int;
   assignedBy: principal;
   personId: PersonId;
   priority: nat;
   status: SuccessorStatus;
 };
type StoryStatus = 
 variant {
   Approved;
   Pending;
   Rejected;
 };
type StoryId = nat;
type Story = 
 record {
   contributor: principal;
   createdAt: int;
   era: opt text;
   evidenceStatus: EvidenceStatus;
   id: StoryId;
   location: opt text;
   relatedArchiveItemIds: vec nat;
   relatedMemberIds: vec text;
   status: StoryStatus;
   storyText: text;
   title: text;
   updatedAt: int;
   year: opt nat;
 };
type StewardRoleStatus = 
 variant {
   Active;
   Removed;
 };
type StewardRecord = 
 record {
   assignedAt: int;
   assignedBy: principal;
   roleStatus: StewardRoleStatus;
   stewardAccountId: principal;
   successorPriority: opt nat;
 };
type StewardIdentity = 
 record {
   accountId: principal;
   canonicalName: text;
   displayName: text;
   personId: PersonId;
 };
type StewardError = 
 variant {
   AlreadySteward;
   LastSteward;
   NotApprovedClaimedMember;
   NotDesignated;
   NotSignedIn;
   NotSteward;
 };
type SourceStatus = 
 variant {
   Copy;
   Original;
   Transcribed;
   Unverified;
 };
type Result__1 = 
 record {
   hasMore: bool;
   rows: vec vec Cell;
 };
type Result_9 = 
 variant {
   err: StewardError;
   ok: StewardRecord;
 };
type Result_8 = 
 variant {
   err: RelationshipError;
   ok: RelationshipRequest;
 };
type Result_7 = 
 variant {
   err: RemoveError;
   ok;
 };
type Result_6 = 
 variant {
   err: RelationshipAdminError;
   ok;
 };
type Result_5 = 
 variant {
   err: StewardError;
   ok;
 };
type Result_4 = 
 variant {
   err: ClaimError;
   ok: ProfileClaim;
 };
type Result_3 = 
 variant {
   err: RemovalError;
   ok: ProfileRemovalRequest;
 };
type Result_2 = 
 variant {
   err: ArchiveError;
   ok;
 };
type Result_19 = 
 variant {
   err: Error;
   ok;
 };
type Result_18 = 
 variant {
   err: AccountError;
   ok: Account;
 };
type Result_17 = 
 variant {
   err: RelationshipAdminError;
   ok: Relationship;
 };
type Result_16 = 
 variant {
   err: CreateError;
   ok: PersonProfile;
 };
type Result_15 = 
 variant {
   err: StewardError;
   ok: SuccessorDesignation;
 };
type Result_14 = 
 variant {
   err: AccountError;
   ok: AccountId;
 };
type Result_13 = 
 variant {
   err: AccountError;
   ok: AuthMethods;
 };
type Result_12 = 
 variant {
   err: MergeError;
   ok: MergeResult;
 };
type Result_11 = 
 variant {
   err: MergeError;
   ok;
 };
type Result_10 = 
 variant {
   err: DeleteError;
   ok;
 };
type Result_1 = 
 variant {
   err: MessageError;
   ok: Message;
 };
type Result = 
 variant {
   err: EditError;
   ok: PersonProfile;
 };
type Resolution = 
 record {
   resolvedAt: int;
   resolvedBy: principal;
   summary: text;
   supportingEvidence: vec text;
 };
type ReportedMessageView = 
 record {
   message: Message;
   report: Report;
 };
type ReportStatus = 
 variant {
   Dismissed;
   Pending;
   Reviewed;
 };
type ReportId = nat;
type Report = 
 record {
   createdAt: Timestamp;
   reason: text;
   reportId: ReportId;
   reportedMessageId: MessageId;
   reportingAccountId: AccountId;
   status: ReportStatus;
 };
type ReplyId = nat;
type Reply = 
 record {
   authorAccountId: AccountId;
   authorPersonId: PersonId;
   body: text;
   createdAt: Timestamp;
   postId: PostId;
   replyId: ReplyId;
 };
type RemoveError = 
 variant {
   NotSignedIn;
   ProfileNotFound;
 };
type RemovalError = 
 variant {
   AlreadyPending;
   DeceasedProfile;
   NotOwner;
   NotSignedIn;
   ProfileNotFound;
 };
type RelationshipType = 
 variant {
   Child;
   Parent;
   Sibling;
   SpousePartner;
 };
type RelationshipStatus = 
 variant {
   Confirmed;
   Disputed;
   Pending;
 };
type RelationshipRequestStatus = 
 variant {
   Approved;
   Pending;
   Rejected;
 };
type RelationshipRequest = 
 record {
   id: nat;
   proposedRelationship: RelationshipType;
   relatedPersonId: PersonId;
   requestingPersonId: PersonId;
   reviewedDate: opt int;
   reviewer: opt principal;
   status: RelationshipRequestStatus;
   submittedDate: int;
 };
type RelationshipError = 
 variant {
   DuplicateRequest;
   NotSignedIn;
   PersonNotFound;
 };
type RelationshipAdminError = 
 variant {
   DuplicateRelationship;
   NotSignedIn;
   PersonNotFound;
   RelationshipNotFound;
 };
type Relationship = 
 record {
   fromPersonId: PersonId;
   id: nat;
   relationshipType: RelationshipType;
   status: RelationshipStatus;
   toPersonId: PersonId;
 };
type RecipeStatus = 
 variant {
   Approved;
   Archived;
   Pending;
   Rejected;
 };
type RecipeId = nat;
type Recipe = 
 record {
   aiDerivedText: opt text;
   contributorAccountId: principal;
   createdAt: int;
   era: opt text;
   evidenceStatus: EvidenceStatus;
   extractedIngredients: opt vec text;
   familyBranch: opt text;
   familyStory: opt text;
   ingredients: vec text;
   instructions: text;
   linkedMediaIds: vec nat;
   location: opt text;
   ocrText: opt text;
   originatingPersonId: text;
   privacyLevel: PrivacyLevel;
   recipeId: RecipeId;
   relatedPersonIds: vec text;
   shortDescription: text;
   status: RecipeStatus;
   tags: vec text;
   title: text;
   transcript: opt text;
   updatedAt: int;
   year: opt nat;
 };
type ProfileRemovalStatus = 
 variant {
   Approved;
   Pending;
   Rejected;
 };
type ProfileRemovalRequest = 
 record {
   id: nat;
   personId: PersonId;
   reason: text;
   requestingUserId: principal;
   reviewedBy: opt principal;
   reviewedDate: opt int;
   status: ProfileRemovalStatus;
   submittedDate: int;
 };
type ProfileEdits = 
 record {
   birthDate: opt text;
   birthInfo: opt text;
   birthplace: opt text;
   currentLocation: opt text;
   firstName: opt text;
   lastName: opt text;
   livingStatus: opt LivingStatus;
   longerStory: opt text;
   middleName: opt text;
   nickname: opt text;
   occupation: opt text;
   preferredName: opt text;
   privacySettings: opt text;
   shortBio: opt text;
   story: opt text;
   suffix: opt text;
   timeline: opt vec text;
 };
type ProfileClaimStatus = 
 variant {
   Approved;
   Pending;
   Rejected;
 };
type ProfileClaim = 
 record {
   id: nat;
   personId: PersonId;
   requestingUserId: principal;
   reviewedBy: opt principal;
   reviewedDate: opt int;
   status: ProfileClaimStatus;
   submittedDate: int;
 };
type PrivacyScope = variant {FamilyOnly;};
type PrivacyLevel = 
 variant {
   FamilyOnly;
   Private;
   Public;
 };
type PostType = 
 variant {
   Announcement;
   FamilyQuestion;
   General;
   Memorial;
   Other;
   PhotoIdentification;
   Recipe;
   ResearchHistory;
   ReunionEvent;
 };
type PostStatus = 
 variant {
   Active;
   Archived;
 };
type PostId = nat;
type Post = 
 record {
   authorAccountId: AccountId;
   authorPersonId: PersonId;
   body: text;
   createdAt: Timestamp;
   linkedMediaIds: vec nat;
   postId: PostId;
   postType: PostType;
   privacyScope: PrivacyScope;
   relatedPersonIds: vec PersonId;
   status: PostStatus;
   title: opt text;
   updatedAt: Timestamp;
 };
type PhotoId = nat;
type Photo = 
 record {
   "blob": ExternalBlob;
   filename: text;
   id: PhotoId;
   mimeType: text;
   uploadedAt: int;
   uploadedBy: principal;
 };
type PersonProfile = 
 record {
   birthDate: opt text;
   birthInfo: opt text;
   birthplace: opt text;
   claimStatus: ClaimStatus;
   claimedByUserId: opt principal;
   currentLocation: opt text;
   firstName: opt text;
   lastName: opt text;
   livingStatus: LivingStatus;
   longerStory: opt text;
   middleName: opt text;
   name: text;
   nickname: opt text;
   occupation: opt text;
   personId: PersonId;
   preferredName: opt text;
   privacySettings: opt text;
   shortBio: opt text;
   story: opt text;
   suffix: opt text;
   timeline: opt vec text;
 };
type PersonMatch = 
 record {
   name: text;
   parents: vec text;
   personId: PersonId;
 };
type PersonId = text;
type OralHistorySpeaker = 
 record {
   name: text;
   personId: opt text;
 };
type NotificationType = 
 variant {
   BoardMention;
   BoardReply;
   NewMessage;
   ProfileClaimRequested;
   ProfileClaimReviewed;
   RelationshipRequested;
   RelationshipReviewed;
 };
type NotificationId = nat;
type Notification = 
 record {
   createdAt: int;
   id: nat;
   message: text;
   notificationType: NotificationType;
   read: bool;
   recipient: principal;
 };
type MysteryStatus = 
 variant {
   Open;
   PartiallyResolved;
   Researching;
   Resolved;
 };
type MysteryId = nat;
type MysteryContributionType = 
 variant {
   Lead;
   Memory;
   Note;
   Source;
 };
type MysteryContributionStatus = 
 variant {
   Approved;
   Pending;
   Rejected;
 };
type MysteryContributionId = nat;
type MysteryContribution = 
 record {
   contributionType: MysteryContributionType;
   contributor: principal;
   createdAt: int;
   id: MysteryContributionId;
   mysteryId: MysteryId;
   reviewedAt: opt int;
   reviewedBy: opt principal;
   status: MysteryContributionStatus;
   "text": text;
 };
type Mystery = 
 record {
   contributor: principal;
   createdAt: int;
   description: text;
   id: MysteryId;
   knownFacts: vec text;
   possibilities: vec text;
   relatedArchiveItemIds: vec nat;
   relatedBranchId: opt text;
   relatedMemberIds: vec text;
   relatedSourceIds: vec nat;
   resolution: opt Resolution;
   status: MysteryStatus;
   title: text;
   updatedAt: int;
 };
type MessageStatus = 
 variant {
   Blocked;
   Sent;
 };
type MessageId = nat;
type MessageError = 
 variant {
   BlockedByRecipient;
   CannotMessageSelf;
   ConversationNotFound;
   NotApprovedMember;
   NotParticipant;
   NotSignedIn;
   RecipientArchived;
   RecipientNotClaimed;
   RecipientNotFound;
 };
type Message = 
 record {
   body: text;
   conversationId: ConversationId;
   createdAt: Timestamp;
   messageId: MessageId;
   readAt: opt Timestamp;
   senderAccountId: AccountId;
   senderPersonId: PersonId;
   status: MessageStatus;
 };
type MergeResult = 
 record {
   archivedPersonId: PersonId;
   canonicalPersonId: PersonId;
   conflicts: vec MergeConflict;
 };
type MergeError = 
 variant {
   NotDuplicate;
   NotSignedIn;
   ProfileNotFound;
   SameProfile;
 };
type MergeConflictStatus = 
 variant {
   Pending;
   Resolved;
 };
type MergeConflict = 
 record {
   alternateValue: text;
   canonicalValue: text;
   field: text;
   id: nat;
   resolvedAt: opt int;
   resolvedBy: opt principal;
   status: MergeConflictStatus;
 };
type LivingStatus = 
 variant {
   Deceased;
   Living;
 };
type ExternalBlob = blob;
type EvidenceStatus = 
 variant {
   Documented;
   FamilyHistory;
   PersonalMemory;
   Unresolved;
 };
type Error = 
 variant {
   AmbiguousAttribute: record {
                         field: text;
                         sources: vec text;
                       };
   FrontendOriginMismatch: record {
                             expected: vec text;
                             got: text;
                           };
   FrontendOriginsNotConfigured;
   MalformedCandid;
   MissingField: text;
   MixedSsoSources: record {
                      otherKeys: vec text;
                      ssoKeys: vec text;
                    };
   NoAttributes;
   Stale: record {ageNs: nat;};
   UnknownNonce;
   UntrustedSsoSource: record {domain: text;};
 };
type EditError = 
 variant {
   DeceasedProfile;
   NotOwner;
   NotSignedIn;
   ProfileNotFound;
 };
type DuplicatePair = 
 record {
   candidateA: DuplicateCandidate;
   candidateB: DuplicateCandidate;
 };
type DuplicateCandidate = 
 record {
   archiveLinks: vec text;
   birthDate: opt text;
   children: vec text;
   claimStatus: text;
   deathDate: opt text;
   name: text;
   ownerAccount: opt principal;
   parents: vec text;
   personId: PersonId;
   photoCount: nat;
   sourceCount: nat;
   spouses: vec text;
   timelineCount: nat;
 };
type DeleteError = 
 variant {
   ConfirmationRequired;
   HasApprovedRelationships;
   HasArchiveItems;
   HasMedia;
   HasOwnershipHistory;
   HasTimeline;
   NotSignedIn;
   ProfileNotFound;
 };
type CreateError = variant {NotSignedIn;};
type ConversationView = 
 record {
   conversationId: ConversationId;
   messages: vec Message;
   participantDisplayNames: vec text;
   participantPersonIds: vec PersonId;
 };
type ConversationSummary = 
 record {
   conversationId: ConversationId;
   latestMessageAt: Timestamp;
   latestMessagePreview: text;
   otherDisplayName: text;
   otherPersonId: PersonId;
   unreadCount: nat;
 };
type ConversationId = nat;
type ClaimStatus = 
 variant {
   Claimed;
   Unclaimed;
 };
type ClaimError = 
 variant {
   AlreadyClaimed;
   AlreadyPending;
   DeceasedProfile;
   NotSignedIn;
   ProfileNotFound;
 };
type ChapterMarker = 
 record {
   timestamp: nat;
   title: text;
 };
type Cell = 
 record {
   name: text;
   value: Value;
 };
type AuthMethods = 
 record {
   apple: bool;
   google: bool;
 };
type AuthMethod = 
 variant {
   Apple;
   Google;
 };
type AuditEntry = 
 record {
   actionType: AuditActionType;
   actorAccountId: principal;
   affectedPersonIds: vec PersonId;
   id: nat;
   summary: text;
   timestamp: int;
 };
type AuditActionType = 
 variant {
   BoardPostArchived;
   BoardPostRestored;
   BoardReplyRemoved;
   ClaimApproved;
   ClaimRejected;
   DuplicateMerged;
   ProfileArchived;
   ProfilePermanentlyDeleted;
   ProfileRemovalRequested;
   ProfileRemovalReviewed;
   ProfileRestored;
   RelationshipAdded;
   RelationshipRemoved;
   RelationshipRequestApproved;
   RelationshipRequestPending;
   RelationshipRequestRejected;
   RelationshipTypeCorrected;
   StewardPromoted;
   StewardRemoved;
   SuccessorActivated;
   SuccessorDesignated;
 };
type ArchiveItemType = 
 variant {
   Audio;
   Document;
   Other;
   Photo;
   Research;
   Video;
   WorkBusiness;
   WrittenStoryNote;
 };
type ArchiveItemStatus = 
 variant {
   Approved;
   Pending;
   Rejected;
 };
type ArchiveItemId = nat;
type ArchiveItemClassification = 
 variant {
   OralHistory;
   Standard;
 };
type ArchiveItem = 
 record {
   aiSummary: opt text;
   "blob": ExternalBlob;
   chapterMarkers: opt vec ChapterMarker;
   classification: ArchiveItemClassification;
   contributor: principal;
   createdAt: int;
   description: text;
   era: text;
   extractedNames: opt vec text;
   id: ArchiveItemId;
   itemType: ArchiveItemType;
   primarySpeaker: opt OralHistorySpeaker;
   privacyLevel: PrivacyLevel;
   relatedBranchId: opt text;
   relatedMemberIds: vec text;
   searchableTranscript: opt text;
   sourceStatus: SourceStatus;
   status: ArchiveItemStatus;
   tags: vec text;
   title: text;
   transcript: opt text;
   year: opt nat;
 };
type ArchiveError = 
 variant {
   AlreadyArchived;
   NotArchived;
   NotSignedIn;
   ProfileNotFound;
 };
type AccountId = principal;
type AccountError = 
 variant {
   AccountNotFound;
   NotSignedIn;
 };
type Account = 
 record {
   authMethods: vec AuthMethod;
   createdAt: int;
   id: AccountId;
 };
service : {
  _immutableObjectStorageBlobsAreLive: (hashes: vec blob) -> (vec bool) query;
  _immutableObjectStorageBlobsToDelete: () -> (vec blob) query;
  _immutableObjectStorageConfirmBlobDeletion: (blobs: vec blob) -> ();
  _immutableObjectStorageCreateCertificate: (blobHash: text) ->
   (_ImmutableObjectStorageCreateCertificateResult);
  _immutableObjectStorageRefillCashier: (refillInformation:
   opt _ImmutableObjectStorageRefillInformation) ->
   (_ImmutableObjectStorageRefillResult);
  _immutableObjectStorageUpdateGatewayPrincipals: () -> ();
  _initialize_access_control: () -> ();
  _internet_identity_sign_in_finish: () -> (Result_19);
  _internet_identity_sign_in_start: () -> (blob);
  /// Activates/promotes a designated successor into the active steward role.
  /// Family Steward only.
  activateSuccessor: (personId: PersonId) -> (Result_9);
  /// Adds a one-level reply to a board post. Approved family members only.
  /// Creates a reply notification for the post author.
  addBoardReply: (postId: PostId, body: text) -> (Reply);
  /// Adds a canonical story directly (steward only), already approved.
  addCanonicalStory: (title: text, storyText: text, relatedMemberIds:
   vec text, era: opt text, year: opt nat, location: opt text,
   evidenceStatus: EvidenceStatus, relatedArchiveItemIds: vec nat) -> (
   Story);
  /// Uploads a new photo to a person's gallery. The signed-in caller is
  /// recorded as the uploader. When the gallery has no profile photo yet, the
  /// newly added photo is automatically set as the profile photo. Returns the
  /// stored photo.
  addPhoto: (personId: PersonId, filename: text, mimeType: text, "blob":
   ExternalBlob) -> (Photo);
  /// Adds a missing relationship to the shared family graph. Family Steward
  /// only.
  addRelationship: (fromPersonId: PersonId, toPersonId: PersonId,
   relationshipType: RelationshipType) -> (Result_17);
  /// Approves a pending archive item (admin only). Returns the updated item, or
  /// `null` when the item does not exist or is not pending.
  approveArchiveItem: (id: ArchiveItemId) -> (opt ArchiveItem);
  /// Approves a pending profile claim, marking the profile claimed and
  /// associating it with the requesting user. Family Steward only.
  approveProfileClaim: (claimId: nat) -> (opt ProfileClaim);
  /// Approves a profile removal request, archiving the profile. Family Steward
  /// only.
  approveProfileRemoval: (requestId: nat) -> (opt ProfileRemovalRequest);
  /// Approves a pending recipe (steward only). Returns the updated recipe, or
  /// `null` when the recipe does not exist or is not pending.
  approveRecipe: (id: RecipeId) -> (opt Recipe);
  /// Approves a relationship request, adding/confirming the relationship in the
  /// shared family graph. Family Steward only.
  approveRelationshipRequest: (requestId: nat) -> (opt RelationshipRequest);
  /// Approves a pending story (steward only). Returns the updated story, or
  /// `null` when the story does not exist or is not pending.
  approveStory: (id: StoryId) -> (opt Story);
  /// Archives (hides) a board post. The author or a Family Steward may archive.
  /// Governance actions create audit entries.
  archiveBoardPost: (postId: PostId) -> (opt Post);
  /// Archives a profile, removing it from normal family browsing while
  /// preserving relationships, media, timeline, sources, and ownership history.
  /// Family Steward only.
  archiveProfile: (personId: PersonId) -> (Result_2);
  assignCallerUserRole: (user: principal, role: UserRole) -> ();
  /// Binds an authentication method (Google or Apple) to the signed-in caller's
  /// account. The account id is the caller's stable principal, so the same
  /// person profile stays intact if the provider changes.
  bindAuthMethod: (method: AuthMethod) -> (Result_18);
  /// Blocks another member, preventing them from sending new messages to the
  /// caller. Approved family members only.
  blockUser: (blockedAccountId: principal) -> ();
  /// Returns whether the signed-in caller may message the person identified by
  /// `personId`: the viewer is signed in, the target has an active linked
  /// account, the target is not the viewer, and the target is not archived.
  /// Unclaimed profiles are never messageable. Drives the Message button on a
  /// living claimed Person Profile.
  canMessagePerson: (personId: text) -> (bool) query;
  /// Corrects the relationship type of an existing relationship. Family Steward
  /// only.
  correctRelationshipType: (relationshipId: nat, relationshipType:
   RelationshipType) -> (Result_17);
  /// Creates a board post with a type, optional title, body, related family
  /// members, and optional linked existing Archive/media ids. Approved family
  /// members only. Creates a mention notification for related members where
  /// appropriate.
  createBoardPost: (postType: PostType, title: opt text, body: text,
   relatedPersonIds: vec text, linkedMediaIds: vec nat) -> (Post);
  /// Creates a canonical mystery directly (steward only).
  createCanonicalMystery: (title: text, description: text, relatedMemberIds:
   vec text, relatedBranchId: opt text, knownFacts: vec text, possibilities:
   vec text, relatedSourceIds: vec nat, relatedArchiveItemIds: vec nat,
   status: MysteryStatus) -> (Mystery);
  /// "Add Myself to This Family": creates a minimal person profile for a user
  /// who does not already exist. The user must then connect to an existing
  /// family member via a relationship request.
  createMyself: (name: text) -> (Result_16);
  /// Designates an approved claimed family member as a successor steward with a
  /// priority/order. A successor is a designation only until activated.
  /// Family Steward only.
  designateSuccessor: (personId: PersonId, priority: nat) -> (Result_15);
  execute: (qJson: text) -> (Result__1) query;
  getApiDoc: () -> (text) query;
  /// Returns a single active board post by id. Approved family members only.
  getBoardPost: (postId: PostId) -> (opt Post) query;
  getCallerUserRole: () -> (UserRole) query;
  /// Returns a full conversation view for a participant. Only participants may
  /// read a conversation.
  getConversation: (conversationId: ConversationId) ->
   (opt ConversationView) query;
  /// Returns the stable account id of the signed-in caller. Anonymous callers
  /// receive #NotSignedIn.
  getMyAccountId: () -> (Result_14) query;
  /// Returns the authentication methods bound to the signed-in caller's account.
  getMyAuthMethods: () -> (Result_13) query;
  /// Returns the signed-in caller's own linked/claimed Person Profile, or, when
  /// none is linked, the caller's pending profile (created via `createMyself` or
  /// with a pending claim by the caller). Returns `null` when the caller has no
  /// profile. Not gated to admin — any signed-in caller may query their own
  /// profile.
  getMyProfile: () -> (opt PersonProfile) query;
  /// Returns the current caller's own claim on a specific profile, or `null`
  /// when the caller has no claim on that profile. Not gated to admin — any
  /// signed-in caller may query their own claim.
  getMyProfileClaim: (personId: PersonId) -> (opt ProfileClaim) query;
  /// Returns the signed-in caller's own pending relationship requests (requests
  /// involving a profile the caller owns or created). Not gated to admin — any
  /// signed-in caller may query their own pending relationship state.
  getMyRelationshipRequests: () -> (vec RelationshipRequest) query;
  /// Returns the count of all current pending review items (archive/media,
  /// video/audio, recipes, recipe media, and other contribution types) for the
  /// Steward-facing Pending Contributions badge. Family Steward only. The count
  /// is derived from canonical pending data, so it increments on new pending
  /// items and decrements on Approve/Reject automatically.
  getPendingContributionsCount: () -> (nat) query;
  /// Returns the ownership/lifecycle state of a person profile, or `null` when
  /// the person is not tracked.
  getPersonProfile: (personId: PersonId) -> (opt PersonProfile) query;
  /// Returns the person's current profile photo, or `null` when none is set.
  getProfilePhoto: (personId: PersonId) -> (opt Photo) query;
  /// Returns a single recipe by id, or `null` when it does not exist or is not
  /// visible to the caller. Private recipes are only visible to their
  /// contributor or a Family Steward; non-approved recipes are only visible to
  /// a Family Steward.
  getRecipe: (id: RecipeId) -> (opt Recipe) query;
  /// Returns a single relationship request by id.
  getRelationshipRequest: (id: nat) -> (opt RelationshipRequest) query;
  /// Returns the reported message content for a report. Family Steward only;
  /// reported message content is visible only when a report is filed. Stewards
  /// cannot browse arbitrary private conversations.
  getReportedMessage: (reportId: ReportId) -> (opt ReportedMessageView) query;
  /// Returns a warning encouraging successor designation when only one steward
  /// exists, or `null` when there are multiple stewards. Family Steward only.
  getSingleStewardWarning: () -> (opt text) query;
  isCallerAdmin: () -> (bool) query;
  /// Lists all archive items in approved state (visible in the archive).
  listApprovedArchiveItems: () -> (vec ArchiveItem) query;
  /// Lists all approved recipes visible to the caller. Private recipes are only
  /// visible to their contributor or a Family Steward.
  listApprovedRecipes: () -> (vec Recipe) query;
  /// Lists all approved stories (visible to viewers).
  listApprovedStories: () -> (vec Story) query;
  /// Returns the ids of all archived profiles so normal family browsing can
  /// filter them out. Not gated to stewards — any caller may read archived ids.
  listArchivedProfileIds: () -> (vec PersonId) query;
  /// Lists all archived profiles. Family Steward only.
  listArchivedProfiles: () -> (vec PersonProfile) query;
  /// Returns the governance audit log. Audit History is strictly steward-only.
  listAuditHistory: () -> (vec AuditEntry) query;
  /// Lists the account ids the caller has blocked. Approved family members only.
  listBlockedUsers: () -> (vec principal) query;
  /// Lists active board posts, newest first, optionally filtered by post type.
  /// Approved family members only.
  listBoardPosts: (filter: opt PostType) -> (vec Post) query;
  /// Lists the replies to a board post, chronologically. Approved family members
  /// only.
  listBoardReplies: (postId: PostId) -> (vec Reply) query;
  /// Lists all confirmed relationships for the frontend to merge into the
  /// shared family graph.
  listConfirmedRelationships: () -> (vec Relationship) query;
  /// Returns the signed-in caller's inbox: one summary per conversation they
  /// participate in, newest activity first. Approved family members only.
  listConversations: () -> (vec ConversationSummary) query;
  /// Lists suspected duplicate Person records with comparison data. Family
  /// Steward only.
  listDuplicateCandidates: () -> (vec DuplicatePair) query;
  /// Returns the eligible promotion/successor candidate list: all people who
  /// are living, have an APPROVED/CLAIMED profile, are linked to a valid
  /// account, are not already an active Steward, and are not archived. This is
  /// data-driven — as additional family members claim and receive approval they
  /// automatically appear without code changes. Family Steward only.
  listEligibleStewardCandidates: () -> (vec StewardIdentity) query;
  /// Returns the person ids of every other member the signed-in caller may
  /// message: living, claimed, linked to an active account, not archived, and
  /// not the caller. Not gated to stewards — any approved member may read it, so
  /// the Private Messages inbox can determine whether any other eligible member
  /// exists. Data-driven: as another relative claims and receives approval they
  /// automatically appear without code changes.
  listMessageableMembers: () -> (vec text) query;
  /// Lists all mysteries (visible to viewers).
  listMysteries: () -> (vec Mystery) query;
  /// Lists in-app notification records for the signed-in caller.
  listNotifications: () -> (vec Notification) query;
  /// Lists all archive items in pending state (admin only).
  listPendingArchiveItems: () -> (vec ArchiveItem) query;
  /// Lists all mystery contributions in pending state (steward only).
  listPendingMysteryContributions: () -> (vec MysteryContribution) query;
  /// Lists all recipes in pending state (steward only).
  listPendingRecipes: () -> (vec Recipe) query;
  /// Lists all stories in pending state (steward only).
  listPendingStories: () -> (vec Story) query;
  /// Returns the current relationships for a person. Family Steward only.
  listPersonRelationships: (personId: PersonId) -> (vec Relationship) query;
  /// Lists all uploaded photos for a person, in upload order.
  listPhotos: (personId: PersonId) -> (vec Photo) query;
  /// Lists all profile claim requests for the Family Steward review area.
  listProfileClaims: () -> (vec ProfileClaim) query;
  /// Lists all profile removal requests for steward review. Family Steward only.
  listProfileRemovalRequests: () -> (vec ProfileRemovalRequest) query;
  /// Lists recipes linked to a person, whether as the originating member or a
  /// related member. Returns only approved recipes visible to the caller;
  /// private recipes are only visible to their contributor or a Family Steward.
  listRecipesForPerson: (personId: text) -> (vec Recipe) query;
  /// Lists all relationship requests for the Family Steward review area.
  listRelationshipRequests: () -> (vec RelationshipRequest) query;
  /// Lists all reports. Family Steward only.
  listReports: () -> (vec Report) query;
  /// Returns each current Steward and designated Successor enriched with the
  /// linked approved Person identity (personId, preferred/display name, and
  /// canonical full person name), resolved via steward accountId -> approved
  /// linked personId (PersonProfile.claimedByUserId) -> canonical Person
  /// Profile. The internal account id is carried only for authorization/audit.
  /// Family Steward only.
  listStewardIdentities: () -> (vec StewardIdentity) query;
  /// Lists all current Family Stewards with role status and account identity.
  /// Family Steward only.
  listStewards: () -> (vec StewardRecord) query;
  /// Lists all successor designations. Family Steward only.
  listSuccessors: () -> (vec SuccessorDesignation) query;
  /// Lists timeline events aggregated from existing canonical data (visible to
  /// viewers).
  listTimelineEvents: () -> (vec TimelineEvent) query;
  /// Marks all of the caller's messages in a conversation as read. Only
  /// participants may mark a conversation read.
  markConversationRead: (conversationId: ConversationId) -> ();
  /// Marks a mystery resolved (steward only), recording the resolution summary
  /// and supporting evidence while preserving the prior theories/history.
  /// Returns the updated mystery, or `null` when it does not exist.
  markMysteryResolved: (id: MysteryId, summary: text, supportingEvidence:
   vec text) -> (opt Mystery);
  /// Marks one of the signed-in caller's notifications as read. Returns the
  /// updated notification, or `null` when it does not exist or is not addressed
  /// to the caller.
  markNotificationRead: (id: NotificationId) -> (opt Notification);
  /// Merges two duplicate profiles into one canonical record, preserving all
  /// valid relationships, media, timeline, stories, sources, archive references,
  /// and ownership/claim history without duplicating shared items. Conflicting
  /// fields are preserved as conflict/review items. The merged-away record is
  /// archived rather than hard-deleted. Family Steward only.
  mergeProfiles: (canonicalPersonId: PersonId, mergedAwayPersonId:
   PersonId) -> (Result_12);
  /// Marks two suspected duplicates as not a duplicate. Family Steward only.
  notDuplicate: (personIdA: PersonId, personIdB: PersonId) -> (Result_11);
  /// Permanently deletes a profile only when it is empty of archive items,
  /// media, timeline/history, approved relationships, and ownership history,
  /// and explicit confirmation is given. Family Steward only.
  permanentlyDeleteProfile: (personId: PersonId, confirmation: bool) ->
   (Result_10);
  /// Promotes an existing approved claimed family member to Family Steward.
  /// Family Steward only.
  promoteToSteward: (personId: PersonId) -> (Result_9);
  /// Proposes a new relationship between two people. The request starts pending
  /// and is never treated as confirmed until a Family Steward approves it.
  proposeRelationship: (fromPersonId: PersonId, toPersonId: PersonId,
   relationshipType: RelationshipType) -> (Result_8);
  /// Publishes a canonical recipe directly (steward only), already approved.
  /// This is the steward-only add flow; it does not create a second Recipe on
  /// approval.
  publishRecipe: (title: text, shortDescription: text, originatingPersonId:
   text, relatedPersonIds: vec text, era: opt text, year: opt nat, location:
   opt text, familyBranch: opt text, ingredients: vec text, instructions:
   text, familyStory: opt text, tags: vec text, privacyLevel: PrivacyLevel,
   evidenceStatus: EvidenceStatus, linkedMediaIds: vec nat) -> (Recipe);
  /// Rejects a pending archive item (admin only). Returns the updated item, or
  /// `null` when the item does not exist or is not pending.
  rejectArchiveItem: (id: ArchiveItemId) -> (opt ArchiveItem);
  /// Rejects a pending profile claim. Family Steward only.
  rejectProfileClaim: (claimId: nat) -> (opt ProfileClaim);
  /// Rejects a profile removal request. Family Steward only.
  rejectProfileRemoval: (requestId: nat) -> (opt ProfileRemovalRequest);
  /// Rejects a pending recipe (steward only). Returns the updated recipe, or
  /// `null` when the recipe does not exist or is not pending.
  rejectRecipe: (id: RecipeId) -> (opt Recipe);
  /// Rejects a relationship request. Family Steward only.
  rejectRelationshipRequest: (requestId: nat) -> (opt RelationshipRequest);
  /// Rejects a pending story (steward only). Returns the updated story, or
  /// `null` when the story does not exist or is not pending.
  rejectStory: (id: StoryId) -> (opt Story);
  /// Removes a reply. Family Steward only. Governance actions create audit
  /// entries.
  removeBoardReply: (replyId: ReplyId) -> (opt Reply);
  /// Removes a duplicate test-created profile and any pending relationship
  /// requests or claims tied only to it, preserving the original profile, the
  /// confirmed family graph, and the signed-in account. Family Steward only.
  removeDuplicateProfile: (personId: PersonId) -> (Result_7);
  /// Removes a photo from a person's gallery. Returns `true` when a photo was
  /// removed. If the removed photo was the profile photo, the profile photo is
  /// cleared.
  removePhoto: (personId: PersonId, photoId: PhotoId) -> (bool);
  /// Removes an incorrect relationship from the shared family graph. Family
  /// Steward only.
  removeRelationship: (relationshipId: nat) -> (Result_6);
  /// Removes the steward role from another steward, never allowing the last
  /// steward to be removed. Family Steward only.
  removeSteward: (stewardAccountId: principal) -> (Result_5);
  /// Reports a specific message with a reason. Approved family members only.
  reportMessage: (messageId: MessageId, reason: text) -> (Report);
  /// "This is Me": creates a pending profile claim for an unclaimed living
  /// profile. Requires sign-in; does not grant ownership until approved.
  requestProfileClaim: (personId: PersonId) -> (Result_4);
  /// A claimed living profile owner requests removal of their own profile.
  /// A Family Steward reviews the request.
  requestProfileRemoval: (personId: PersonId, reason: text) -> (Result_3);
  /// Resolves a merge conflict by choosing the canonical display value. Family
  /// Steward only.
  resolveMergeConflict: (conflictId: nat, canonicalValue: text) ->
   (opt MergeConflict);
  /// Restores an archived board post. Family Steward only. Governance actions
  /// create audit entries.
  restoreBoardPost: (postId: PostId) -> (opt Post);
  /// Restores an archived profile to normal family browsing. Family Steward
  /// only.
  restoreProfile: (personId: PersonId) -> (Result_2);
  /// Approves or rejects a pending mystery contribution (steward only). Returns
  /// the updated contribution, or `null` when it does not exist or is not
  /// pending.
  reviewMysteryContribution: (id: MysteryContributionId, approve: bool) ->
   (opt MysteryContribution);
  /// Updates a report's review status. Family Steward only.
  reviewReport: (reportId: ReportId, status: ReportStatus) -> (opt Report);
  schema: () -> (text) query;
  /// Searches the authoritative shared profile data for possible duplicate
  /// matches by name, returning name plus parents when known. Names are
  /// normalized before matching (case-insensitive, punctuation ignored, periods
  /// normalized, extra spaces collapsed, suffix variants recognized, partial/
  /// fuzzy allowed).
  searchPossibleMatches: (name: text) -> (vec PersonMatch) query;
  /// Sends a private message to the person identified by `personId`, reusing the
  /// existing 1:1 conversation when one exists. Approved family members only.
  /// Creates a new-message notification for the recipient. Blocking prevents new
  /// messages from the blocked user.
  sendMessage: (recipientPersonId: text, body: text) -> (Result_1);
  /// Marks the photo with `photoId` as the person's profile photo. Returns the
  /// newly selected photo, or `null` when the photo does not exist.
  setProfilePhoto: (personId: PersonId, photoId: PhotoId) -> (opt Photo);
  /// Returns a relationship request to pending state. Family Steward only.
  setRelationshipRequestPending: (requestId: nat) ->
   (opt RelationshipRequest);
  /// Submits a new archive item. Requires sign-in; the signed-in caller is
  /// recorded as the contributor. The item is stored in pending state and waits
  /// for admin approval before appearing in the archive.
  /// 
  /// `classification` marks the item as Oral History (distinct from `itemType`).
  /// When `#OralHistory`, `primarySpeaker` is required (exactly one primary
  /// speaker); when `#Standard`, `primarySpeaker` must be `null`. The reserved
  /// future-ready fields (transcript, searchable transcript, chapter markers,
  /// AI summary, extracted names) are initialized to `null` and are not
  /// populated by any logic yet.
  submitArchiveItem: (title: text, description: text, itemType:
   ArchiveItemType, "blob": ExternalBlob, era: text, year: opt nat, tags:
   vec text, relatedMemberIds: vec text, relatedBranchId: opt text,
   sourceStatus: SourceStatus, privacyLevel: PrivacyLevel, classification:
   ArchiveItemClassification, primarySpeaker: opt OralHistorySpeaker) ->
   (ArchiveItem);
  /// Submits a mystery contribution (a note, memory, possible lead, or
  /// source/document reference). Requires sign-in; the signed-in caller is
  /// recorded as the contributor. The contribution is stored in pending state
  /// and waits for a Family Steward to review it before altering the canonical
  /// mystery record.
  submitMysteryContribution: (mysteryId: MysteryId, contributionType:
   MysteryContributionType, "text": text) -> (MysteryContribution);
  /// Submits a new recipe. Requires sign-in; the signed-in caller is recorded as
  /// the contributor. The recipe is stored in pending state and waits for a
  /// Family Steward to approve it before becoming visible in Family Recipes.
  submitRecipe: (title: text, shortDescription: text, originatingPersonId:
   text, relatedPersonIds: vec text, era: opt text, year: opt nat, location:
   opt text, familyBranch: opt text, ingredients: vec text, instructions:
   text, familyStory: opt text, tags: vec text, privacyLevel: PrivacyLevel,
   evidenceStatus: EvidenceStatus, linkedMediaIds: vec nat) -> (Recipe);
  /// Submits a new story. Requires sign-in; the signed-in caller is recorded as
  /// the contributor. The story is stored in pending state and waits for a
  /// Family Steward to approve it before becoming visible.
  submitStory: (title: text, storyText: text, relatedMemberIds: vec text,
   era: opt text, year: opt nat, location: opt text, evidenceStatus:
   EvidenceStatus, relatedArchiveItemIds: vec nat) -> (Story);
  /// Unblocks another member, allowing them to message the caller again.
  /// Approved family members only.
  unblockUser: (blockedAccountId: principal) -> ();
  /// Updates the caller's own board post. Approved family members only; the
  /// caller must be the post author.
  updateBoardPost: (postId: PostId, postType: PostType, title: opt text,
   body: text, relatedPersonIds: vec text, linkedMediaIds: vec nat) ->
   (opt Post);
  /// Edits a canonical mystery (steward only). Returns the updated mystery, or
  /// `null` when it does not exist.
  updateCanonicalMystery: (id: MysteryId, title: text, description: text,
   relatedMemberIds: vec text, relatedBranchId: opt text, knownFacts:
   vec text, possibilities: vec text, relatedSourceIds: vec nat,
   relatedArchiveItemIds: vec nat, status: MysteryStatus) -> (opt Mystery);
  /// Edits a canonical story (steward only). Returns the updated story, or
  /// `null` when the story does not exist.
  updateCanonicalStory: (id: StoryId, title: text, storyText: text,
   relatedMemberIds: vec text, era: opt text, year: opt nat, location:
   opt text, evidenceStatus: EvidenceStatus, relatedArchiveItemIds:
   vec nat) -> (opt Story);
  /// Updates an approved owner's own living profile fields. Never rewrites
  /// family relationships directly.
  updateOwnProfile: (personId: PersonId, edits: ProfileEdits) -> (Result);
}
